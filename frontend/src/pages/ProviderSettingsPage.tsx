import ProviderSettings from '../features/providers/ProviderSettings';

export default function ProviderSettingsPage() {
  return (
    <div>
      <h1 className="mb-3 text-sm tracking-widest text-term-muted">PROVIDER SETTINGS · KEYS / PROFILES / HEALTH</h1>
      <ProviderSettings />
    </div>
  );
}
