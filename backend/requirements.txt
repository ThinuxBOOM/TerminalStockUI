fastapi>=0.110,<1
uvicorn[standard]>=0.29
pydantic>=2.5
SQLAlchemy>=2.0
httpx>=0.27
yfinance>=0.2.40
tenacity>=8.2
cryptography>=42.0
redis>=5.0
numpy>=1.26
pandas>=2.0
scikit-learn>=1.3
pytest>=8.0
exchange-calendars>=4.5

# --- Serverless (Vercel) vs Docker profiles ---
# Docker Compose (default): QueuePool + local redis via REDIS_URL; psycopg optional.
# Vercel serverless (see api/requirements.txt + api/index.py): NullPool when
# DATABASE_URL targets Supabase/pgbouncer (:6543) or APP_ENV=production; memory
# cache default, UPSTASH_REDIS_URL/REDIS_URL optional (lazy import, no hard dep).
# api/index.py exposes the FastAPI `app` natively (no adapter needed).
mangum>=0.17  # legacy ASGI adapter (kept for compat; not used by api/index.py)
psycopg[binary]>=3.1  # Supabase Postgres driver (required on Vercel when DATABASE_URL is set)
